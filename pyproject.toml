[project]
name = "mirror-repos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyyaml>=6.0.3",
    "rich-click>=1.9.7",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
